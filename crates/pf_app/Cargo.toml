[package]
name = "pf_app"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
pf_core.workspace = true
pf_render.workspace = true
macroquad.workspace = true
# pf_net (rollback) returns in Phase 2, when local play runs through a GGRS
# session. Its ggrs -> rand -> getrandom chain needs the `getrandom` js
# feature on wasm, or the CI wasm build breaks. Phase 3 adds the matchbox
# WebRTC transport.
