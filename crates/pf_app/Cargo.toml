[package]
name = "pf_app"
version = "0.0.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
pf_core.workspace = true
pf_render.workspace = true
macroquad.workspace = true
# pf_net (rollback) is added back in Phase 3, alongside the matchbox WebRTC
# transport and the wasm `getrandom` js-feature config it requires.
