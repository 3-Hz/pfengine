# Rust
/target
# Cargo.lock is intentionally tracked (binary/app workspace).

# Docs site (Zensical build output + Python venv)
/docs-site/site
.venv

# Per-user local settings for Claude Code and Codex
.claude/settings.local.json
/.codex/

# OS / editor
.DS_Store
*.swp
.idea

# Editor: track shared run/debug configs, ignore user-local settings.
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
